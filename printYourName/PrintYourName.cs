using System;

class Program
   //shortcut Ctrl+K then C(add comment)
   //  sortcut Ctrl+K then U(remove comment)
   /* shortcut to to format whitespace adequately Ctrl + K then D */
   //Rightclick on Sln file> Properties> Startup Project> Current Selection to Build the current opened Program
{
    static void Main()
    {
        Console.WriteLine("Pesho 'Finch' Bozov");
    }
}